package requests;

/**
 * This enum contains keys specifying the one-line text prompts
 * that can be printed to console during interactions in <code>RequestController</code>.
 */
public enum RequestPrompts {

    REQUEST_REPLY_PROMPT,
    USER_REQUESTS,
    ALL_REQUESTS,
    END_OF_REQUESTS,
    REQUEST_CONFIRMATION,
    REQUEST_SELECTION_CANCELLED,
    NO_USER_REQUESTS_ERROR,
    NO_REQUESTS_ERROR,
    INVALID_REQUEST_SELECTION_ERROR,
    ALREADY_REPLIED,
    INVALID_COMMAND_ERROR,
    REPLY_CONFIRMATION,
    TEXT_INPUT_PROMPT,
    DELETION_CONFIRMATION,
    REQUEST_SELECTION_PROMPT,
    REQUEST_MENU_USER,
    REQUEST_MENU_ORG,
}
